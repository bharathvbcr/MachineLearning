# §8.4 rows 1–4 — the framing decision, and what the existing runs already answer

Written 2026-09-04. Closes the framing half of gap **E1** in
`ISSUES_AND_GAPS_2026-08-22.md` ("§8.4 rows 1-4 remain UNANSWERED … Open item is
a framing decision, not compute"). No new compute was used to write this.

## The state of play

E1 ran 250 jobs. Every µP cell failed the bundle's own comparator check, and
backlog **E13** found why: µP's `1/d` attention-logit rule, without a companion
q/k init, leaves attention at ~99.8% of uniform entropy at init against SP's
~89%. minGRU has no attention logits, so the handicap lands on **one arm only** —
which is precisely fatal for a board whose question is attention vs minGRU.

Correcting that one term (`*_spattn` = µP with SP's `1/√d` temperature) brings
the crossings back: `e1_mup` crosses on 0/5 seeds, `e1_mup_spattn` on 5/5.

The backlog then declined to point rows 1–4 at the corrected cells, on the
grounds that `*_spattn` is "µP with SP's attention temperature — a different
parametrization". **That caution was right in general and wrong here**, for a
reason that had not been checked.

## Why `*_spattn` is the faithful µP arm for *this* architecture

`scripts/gpu_bundle.py:333` — `heads_for(width)`, "Head count at a given width,
**holding head_dim fixed**". `HEAD_DIM = 64`, `BASE_WIDTH = 256`,
`TARGET_WIDTH = 768`. Width moves through `n_head`; `head_dim` is 64 at the proxy
width and 64 at the target.

That is decisive for what the `1/d` rule is *for*. µP prescribes `1/d_head`
rather than `1/√d_head` so attention logits stay Θ(1) as **`d_head` grows**.
Here `d_head` does not grow. Under µP's own init (`_mup_init` scales hidden
weights by `1/√width_mult`, i.e. std ∝ `1/√fan_in`, which *is* the µP rule),
`q` and `k` entries are Θ(1) at every width, so `q·k` over a fixed `head_dim` is
width-invariant — under **either** normalization.

So at fixed `head_dim`, `1/d_head` versus `1/√d_head`:

- contributes **nothing** to µP's transfer property — both are width-invariant;
- imposes a **constant** 8× temperature reduction (`1/64` vs `1/8`), identical at
  proxy and target;
- and that constant is **arm-asymmetric**, hitting attention and not minGRU.

The term is inert for the thing rows 1–4 ask about and actively destructive for
the comparison they are asked *of*. `*_spattn` is therefore not a weakened µP —
**it is µP with a term that has no width-transfer role in this architecture set
to the value that does not cripple one arm.** The `1/d` cells are best read as an
ablation of that term.

*Status: the `head_dim`-is-fixed fact is **verified** (`gpu_bundle.py:132-134`,
`:333`). That this makes the attention-scale choice irrelevant to transfer is
**derived**, and a reviewer who insists "µP" means the literal published
prescription including `1/d` may still refuse the repointing. Both readings
should be stated; the recommendation is to repoint and say why.*

## What the existing runs answer under the repointing

SP control first, because it decides whether any of this is readable. Suite-22/24
per-seed bands are early **1.03–1.09M**, late **11.93–12.58M**:

| cell | first crossing (M) | last crossing (M) |
|---|---|---|
| `e1_sp_rerun` (SP, s24, this box) | 1.049 [1.023, 1.075] | 12.369 [12.048, 12.690] |

Both land inside the published bands, so the box and the pipeline reproduce the
paper's SP numbers. Everything below is against that anchor.

| cell | recipe | first (M) | last (M) |
|---|---|---|---|
| `e1_mup_spattn` | s24, 50M cosine | 10.117 [9.931, 10.303] | 10.117 (single crossing) |
| `e1_mup_sched20_spattn` | s23, 20M cosine | 2.775 [1.497, 4.052] | 3.585 [3.475, 3.695] |
| `e1_sp_sched20` | s23, SP | 1.047 [1.021, 1.072] | 14.745 [14.077, 15.413] |
| `e1_mup_bs8_spattn` | s25, batch 8 | 1.447 [1.302, 1.592] | 1.447 (single crossing) |
| `e1_sp_bs8` | s25, SP | — none — | — none — |
| `e1_mup_bs8` | s25, uncorrected µP | — none — | — none — |

### Row 4 — **yes, and it is not about µP at all**

> *Does the batch-8 arm develop a crossing within 7.4M tokens?*

All four batch-8 cells, n=5 each:

| cell | crossing |
|---|---|
| SP, inherited `matrix_lr` 0.025 | **none**, 0/5 seeds |
| µP, broken temperature | **none**, 0/5 seeds |
| µP-spattn, tuned | 1.447M [1.302, 1.592], 5/5 |
| **SP, at SP's own optimum 0.003125** | **3.252M [3.140, 3.364], 5/5** |

`e1_sp_bs8_tuned` is the one that matters. **Plain standard parametrization, no
µP anywhere, just a learning rate 8× lower — and the crossing appears.** So the
"batch 8 never crosses" result in §4.4 is an artifact of the inherited learning
rate, not of the batch size and not of the parametrization.

That is the stronger form of row 4, and the paper can state it without
committing to µP at all: **§4.4's batch effect is a tuning artifact.** The two
tuned cells still differ from each other (1.447M vs 3.252M, disjoint), so
parametrization shifts *where* the crossing lands — but whether one exists is a
tuning question.

**Robustness.** That claim rested on "SP's optimum is 0.003125", which is
sign-consistent for attention and **not** for minGRU — 0.003125 and 0.00625 are
tied inside the seed spread on `e1_sp_basin`, and the crossing is *between* the
arms, so minGRU's learning rate moves it directly. Since the claim withdraws a
paper section, `e1_sp_bs8_perarm` ran each arm at its own best candidate
(attention 0.003125, minGRU 0.00625), which is also §8.1's own prescription:

| batch-8 cell | crossing |
|---|---|
| SP, inherited 0.025 | none, 0/5 |
| SP, both arms 0.003125 | 3.252M [3.140, 3.364], 5/5 |
| **SP, per-arm** | **3.646M [3.521, 3.771], 5/5** |
| µP-spattn, tuned | 1.447M [1.302, 1.592], 5/5 |

The crossing **survives**, so withdrawing §4.4 does not depend on resolving that
argmin. Note the two SP variants are themselves disjoint (3.252 vs 3.646), so
minGRU's learning rate moves the crossing by ~0.4M: the **existence** of a
crossing is robust, its **location** is not, and both sit far inside row 4's
7.4M window. Anyone quoting a batch-8 crossing token should quote the per-arm
cell, not the shared-LR one.

#### The original, weaker reading



> *Does the batch-8 arm develop a crossing within 7.4M tokens under µP?*

`e1_mup_bs8_spattn` crosses at **1.447M [1.302, 1.592]**, 5/5 seeds — a factor of
five inside the 7.4M window. Under SP there is no crossing at all, and under
uncorrected µP there is none either, which is why the ledger still records
`row 4: verdict false`.

**Pre-registered reading, unchanged:** *the batch effect is a tuning artifact,
and suite 14's original 6.6–7.4M window is partially rehabilitated.* §4.4 needs
restating on that basis.

This row survives the LR caveat below. It is an **existence** claim, and the
corrected cell is at a known-suboptimal learning rate — finding a crossing anyway
is evidence in the direction the row asks about, not against it.

### Row 3 — **tested, and it does NOT fire**

> *Do the µP crossings collapse onto one token across schedules?*

`e1_mup_tuned_spattn` ran (2026-09-05). The tuned cells appear to collapse:

| cell (tuned µP, SP temperature) | last crossing (M) |
|---|---|
| s24, 50M cosine | 3.560 [3.458, 3.662] |
| s23, 20M cosine | 3.585 [3.475, 3.695] |

0.7% apart, intervals almost coincident, against SP's 12.369M vs 14.745M with
**disjoint** intervals. Read literally, row 3 fires and §4.3 must be withdrawn.

**It should not be read literally.** The tuned learning rate moves the crossing
from ~12M to ~3.5M, and at 3.5M the two cosines have barely separated:

| schedules compared | at the crossing being read | LR ratio |
|---|---|---|
| 50M vs 20M | µP's 3.560M | **1.07×** |
| 50M vs 20M | SP's 12.369M | 2.69× |

So the "collapse" is consistent with there being no schedule difference left to
detect, rather than with the effect being absent.

**The matched-sensitivity control settles it.** A 6M cosine stopped at 20M puts
the two LRs **2.78×** apart at 3.560M — SP's own sensitivity, 2.69×. Same arms,
same anchor, one recipe field changed (`e1_sp_sched6`, `e1_mup_sched6_spattn`,
20 jobs):

| cell | last crossing (M) |
|---|---|
| µP-spattn, 50M cosine | 3.560 [3.458, 3.662] |
| µP-spattn, 20M cosine | 3.585 [3.475, 3.695] |
| **µP-spattn, 6M cosine** | **4.216 [3.898, 4.534]** |

The 6M cell is **disjoint from both** (3.662 < 3.898, and 3.695 < 3.898). Once
the schedules actually differ where the crossing is read, **the µP crossings
separate.** Row 3's precondition is not met; the apparent collapse was a
schedule-regime artifact.

**§4.3 is not withdrawn.**

### Row 2 — this is the row that fires

> *Crossings move, but the ordering of recipes is preserved.*

Under µP the shorter cosine crosses later — 3.560 (50M) ≈ 3.585 (20M) < 4.216
(6M) — the same direction SP gives (12.369 < 14.745, and under a 6M cosine SP
loses its late crossing entirely, crossing only at 1.048M). The literal 20M-vs-50M
test is *unresolved* on its own because those two overlap; the 6M point is what
resolves the direction.

**Pre-registered reading:** *the schedule effect is real; its magnitude in §4.3
is inflated by mis-tuning and should be restated as an upper bound.* Under µP
the whole 50M→6M span moves the crossing 0.66M, against SP's 2.38M for
50M→20M alone — so "inflated" is the right word and the §4.3 figure is an upper
bound, not a point estimate.

### Row 1 — no

2.762M / 3.560M land in neither the early band (1.03–1.09M) nor the late one
(11.93–12.58M).

### The separator arm says the temperature was worth correcting

`e1_sp_coldattn` — SP everywhere, µP's `1/d` temperature and nothing else —
crosses once at **0.854M** [0.840, 0.868] on 4/5 seeds, where SP proper crosses
twice (1.049M and 12.369M) on 5/5. **That one term destroys the late crossing on
its own.** It does not fully explain uncorrected µP (which crosses on 0 seeds),
but it is potent enough that removing it from the µP cells was not cosmetic.

### Correction to an earlier claim in this document

An earlier revision said every corrected cell runs at the transferred 1×. That
was wrong for two of them: `e1_mup_sched20_spattn` and `e1_mup_bs8_spattn` are
in `MUP_ANCHOR_SUITES` and already ran at the measured optimum. Only the s24 cell
was missing, which is what `e1_mup_tuned_spattn` supplied — so the conclusion
(one missing cell) held, but the reasoning about the others did not.

### Superseded: why rows 1–3 were thought unanswerable

The readouts require `e1_mup_tuned`: µP at its **measured target-width optimum**,
not at the transferred LR. Every corrected cell above is at the transferred
**1×**, and `e1_mup_basin_spattn` (42 jobs) shows 1× is not the optimum — the
interior minimum sits at **4× for attention, 2× for minGRU**, unchanged by the
temperature correction, while attention's whole curve drops ~0.42 nats.

Crossing token is strongly LR-dependent in that basin — 4.37M at 8×, 4.95M at 4×,
6.66M at 2×, 19.25M at 0.5× — so reading rows 1–3 off the 1× cell would repeat
the exact error the correction was made to remove.

**What is missing is one cell:** `e1_mup_tuned_spattn`, n=5 at the measured
optimum. **10 jobs.** That closes rows 1–3.

### The suggestive part, flagged as suggestive

At 1×, the schedule ordering **inverts**. Under SP the 20M cosine crosses *later*
than the 50M cosine (14.745M vs 12.369M) — the §4.3 effect. Under µP-spattn it
crosses *earlier* (3.585M vs 10.117M), with intervals nowhere near overlapping.

If that survives at the tuned LR, none of rows 1–3 fires: not "lands in the
bands" (row 1), not "ordering preserved" (row 2), not "collapses onto one token"
(row 3, the two are disjoint by 6.5M). The outcome would be **off-table** — the
sign of the schedule effect is parametrization-dependent — and the honest move is
the runner's own rule: *report it as such rather than mapping it onto the nearest
row.*

That would be a stronger form of the paper's thesis, not a retreat from it: the
tuning axis is itself a recipe axis. **It is not established by these ten jobs**,
because they sit at a suboptimal LR, and it must not be written up as though it
were.

## Recommendation

1. **Repoint rows 1–4 at the `*_spattn` cells**, with the `head_dim` argument
   above stated in the text and the alternative reading acknowledged.
2. **Answer row 4 now** (YES) and restate §4.4's batch result as a tuning
   artifact.
3. **Run `e1_mup_tuned_spattn`, 10 jobs**, to close rows 1–3. This is the whole
   remaining compute cost of §8.4 — down from the 240-job programme, because the
   other 240 already ran.
4. Until (3) lands, §4.3's schedule effect keeps its current caveat, plus a note
   that the 1× corrected cells hint at an inversion rather than an inflation.

## The one arm nobody has run

Whatever rows 1–3 say, they cannot separate *µP* from *the temperature
correction*: every corrected cell changes both at once. The clean separator is
the mirror arm — **SP everywhere with µP's `1/d` attention temperature**
(`e1_sp_coldattn`, 10 jobs). If SP-with-cold-attention also inverts the schedule
ordering, the inversion is the temperature; if it does not, it is µP.

The config flag for it already exists in the opposite direction
(`mup_sqrt_attn_scale`); the arm needs its counterpart under `mup=False` and is
otherwise config-only.
