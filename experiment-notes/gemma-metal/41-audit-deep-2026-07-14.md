# 41: Deep audit snapshot (2026-07-14)

## Executive summary

- **Question:** An independent read of code + JSON (not prior narrative) would locate why native 31B DFlash showed mean_accept≈0 / exact FAIL and rank the real path to ≥15 / ≥25.
- **Result:** Audit correctly pinned mean_accept≈0 on broken GEMM residual algebra; dual-norm landing moves the snapshot, but MLX serve remains the only measured ≥15/≥25 exact path.
- **Implication:** Use this as evidence for the recorded decision within the tested setup; broader transfer remains an inference.
- **Status:** `done`; evidence confidence **Low**.

## Meta

| Field | Value |
|-------|-------|
| Suite id | `41-audit-deep-2026-07-14` |
| Dates | `2026-07-14` – `2026-07-14` (snapshot) |
| Hardware | Apple M5 Pro · 20 GPU · 64 GB · ~273 GB/s |
| Status | `done` (snapshot; partially superseded) |

## Hypothesis

An independent read of code + JSON (not prior narrative) would locate why native 31B DFlash showed mean_accept≈0 / exact FAIL and rank the real path to ≥15 / ≥25.

## Setup

- Trainer / preset: static audit of `gpu_model.rs` / `dflash.rs` / `step_verify` vs bench JSON
- Fixed knobs: prioritize artifacts listed in the audit index
- Env flags: n/a

## Variants

| Variant | Change |
|---------|--------|
| Snapshot claims | As of audit_deep write |
| Later supersessions | dual-norm landing, latest parity JSON, SESSION fusion |

## Results

| Surface (audit snapshot) | Value |
|--------------------------|-------|
| E4B quiet | **23.92** tok/s, TTFT 141 ms |
| 31B quiet | **6.83** tok/s, TTFT 549 ms, Hot 17.87 GiB |
| Honest 31B DFlash | greedy **~5.84**; DFlash **~1.17** @ bs=5; **mean_accept≈0**; exact **FAIL** |
| Mini steered | ≥ greedy; exact PASS |
| MLX product | **~28–37** tok/s; exact PASS |
| Smoking gun | `step_verify_gemm` missing Gemma4 dual-norm + unused `layer_scalar` |
| Roofline | kernels ~62–100% peak; e2e ~75% overhead @ ~23.9 tok/s |

**Recommendation then:** ship `serve_dflash` for gates now; fix GEMM residual algebra before chasing native accept; park GEMV peel.

### What later artifacts supersede

| Audit claim | Superseded by |
|-------------|----------------|
| GEMM dual-norm / `layer_scalar` missing | `dflash_port.md` step 1c **landing/landed**; diag GEMM≡seq under AO |
| mean_accept≈0 as live tip | `latest_dflash_parity_gates.json` real_31b sweep mean_accept **~0.77–1.0** (still ≪ MLX ~3; streams often mode-locked) |
| exact always FAIL on 31B | latest reports `exact_vs_greedy: true` — treat carefully if unique tokens collapse |
| “GEMM quarantined / .wip only” as live state | Already called stale in audit; confirmed live metallib path |
| Roofline “~20% / 4× GEMV” in bottleneck body | Already corrected in bottleneck CORRECTION + `kernel_roofline_finding.json` |
| Fusion unfinished | `SESSION_RESULTS.md` steps 1–5 landed (bf16 fuse best ~24.9 E4B) |

Still open vs audit: draft proposals ≠ MLX goldens; product native ≥15/≥25 **false**; greet golden **0/16**.

**Interpretation boundary.** The audit’s source observations were concrete at snapshot time, but superseded acceptance/exactness values must not be treated as current measurements.

## Failures

- Historical NaN-collapse / false high accept (`run_dflash_parity_gates_final.json`)
- Stale docs (README Phase 4/6, port “GEMM parked”) during dual-agent drift
- Risk of claiming mini steered ≥greedy as 31B readiness

## Lesson

**Audit correctly pinned mean_accept≈0 on broken GEMM residual algebra; dual-norm landing moves the snapshot, but MLX serve remains the only measured ≥15/≥25 exact path.**

## Reproduction

- **No exact replay command was preserved.**
- Closest runner: a static comparison of `gpu_model.rs`, `dflash.rs`, and `step_verify`.
- Required replay inputs: `docs/audit_deep_2026-07-14.md` plus the explicitly listed later superseding artifacts. Reconstruct every recorded field before comparing outputs.

## Evidence quality

**Confidence: Low.** This is a static point-in-time audit and several claims are explicitly superseded; remaining native parity blockers are still concrete.

## Artifacts

- `Rust_MLKit/gemma-metal/docs/audit_deep_2026-07-14.md` — snapshot source
- Later: `docs/dflash_port.md`, `bench/results/latest_dflash_parity_gates.json`, `bench/results/baselines_2026-07-14/SESSION_RESULTS.md`

## Why this experiment happened

The native DFlash record contained stale documentation, NaN-derived false acceptance, mini-only passes, and changing port state. A source-first audit was needed to separate live evidence from superseded claims and to rank the next engineering action. The preceding notebook context is [40-ddtree-frontier](40-ddtree-frontier.md).

## Experiment story

**Baseline.** The native DFlash record contained stale documentation, NaN-derived false acceptance, mini-only passes, and changing port state. A source-first audit was needed to separate live evidence from superseded claims and to rank the next engineering action. The preceding notebook context is [40-ddtree-frontier](40-ddtree-frontier.md).

**Hypothesis.** An independent read of code + JSON (not prior narrative) would locate why native 31B DFlash showed mean_accept≈0 / exact FAIL and rank the real path to ≥15 / ≥25.

**Test contract.** Trainer / preset: static audit of `gpu_model.rs` / `dflash.rs` / `step_verify` vs bench JSON Fixed knobs: prioritize artifacts listed in the audit index Env flags: n/a

**Variant sequence.** The preserved comparison matrix was: Snapshot claims — As of audit_deep write; Later supersessions — dual-norm landing, latest parity JSON, SESSION fusion.

**Measured turn.** The result board records E4B quiet — **23.92** tok/s, TTFT 141 ms; 31B quiet — **6.83** tok/s, TTFT 549 ms, Hot 17.87 GiB; Honest 31B DFlash — greedy **~5.84**; DFlash **~1.17** @ bs=5; **mean_accept≈0**; exact **FAIL**; Mini steered — ≥ greedy; exact PASS; MLX product — **~28–37** tok/s; exact PASS.

**Turning point and readout.** **Recommendation then:** ship `serve_dflash` for gates now; fix GEMM residual algebra before chasing native accept; park GEMV peel. Still open vs audit: draft proposals ≠ MLX goldens; product native ≥15/≥25 **false**; greet golden **0/16**. **Interpretation boundary.** The audit’s source observations were concrete at snapshot time, but superseded acceptance/exactness values must not be treated as current measurements.

**Failures and surprises.** Historical NaN-collapse / false high accept (`run_dflash_parity_gates_final.json`) Stale docs (README Phase 4/6, port “GEMM parked”) during dual-agent drift Risk of claiming mini steered ≥greedy as 31B readiness

## Decision and aftermath

**Kept:** Audit correctly pinned mean_accept≈0 on broken GEMM residual algebra; dual-norm landing moves the snapshot, but MLX serve remains the only measured ≥15/≥25 exact path. The notebook continues with [50-arch02-metal-native-train](../arch-metal/50-arch02-metal-native-train.md); that link records the downstream experiment, not proof that this suite alone caused it.

## Detailed observations

- The result artifact reports: E4B quiet — **23.92** tok/s, TTFT 141 ms.
- The result artifact reports: 31B quiet — **6.83** tok/s, TTFT 549 ms, Hot 17.87 GiB.
- The result artifact reports: Honest 31B DFlash — greedy **~5.84**; DFlash **~1.17** @ bs=5; **mean_accept≈0**; exact **FAIL**.
- The result artifact reports: Mini steered — ≥ greedy; exact PASS.
- Failure/operational record: Historical NaN-collapse / false high accept (`run_dflash_parity_gates_final.json`)
- Failure/operational record: Stale docs (README Phase 4/6, port “GEMM parked”) during dual-agent drift

## What this does not prove

**Confidence: Low.** This is a static point-in-time audit and several claims are explicitly superseded; remaining native parity blockers are still concrete. The recorded association does not by itself establish transfer to other models, datasets, hardware, seeds, prompts, or budgets. Where the story above connects suite order to motivation, that chronology is explicitly a reconstruction from notebook ordering and linked artifacts, not a quoted contemporaneous rationale.

## See also

- Related suites: [`36-native-dflash-parity-accept`](36-native-dflash-parity-accept.md), [`33-kernel-roofline-overhead`](33-kernel-roofline-overhead.md), [`34-mlx-dflash-product`](34-mlx-dflash-product.md), [`37-golden-token-parity`](37-golden-token-parity.md)

---

[Previous](40-ddtree-frontier.md) · [Index](../00-INDEX.md) · Next
