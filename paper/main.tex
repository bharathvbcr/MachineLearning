% Recipe-Dependent Rankings — arXiv build
%
% Build:  make            (tectonic; downloads only the packages it needs)
%         make latexmk    (pdflatex + bibtex, if you prefer a MacTeX install)
%         make check      (refuse to ship a PDF with unresolved citations)
%         make arxiv      (self-contained submission tarball)
%
% body.tex and refs.tex are GENERATED from
% ../PAPER_2026-08_Recipe_Dependent_Rankings.md, which is the source of truth:
% every number in it is checked against the run records by
% paper/derive_figures.py --check. Edits belong there, not here.
\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{fancyvrb}
\usepackage[hidelinks]{hyperref}

% Repo paths in tables must be breakable or they overflow the column.
\PassOptionsToPackage{hyphens}{url}
\usepackage{url}
\def\UrlBreaks{\do\/\do\_\do\-\do\.}

\usepackage{caption}
\usepackage{adjustbox}

% pandoc emits column specs like p{(\linewidth - 8\tabcolsep) * \real{0.2}}
\usepackage{calc}
\providecommand{\real}[1]{#1}

% pandoc's LaTeX writer assumes these come from its default template. We supply
% only the body, so they are defined here.
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}
\providecommand{\pandocbounded}[1]{#1}

% Pandoc emits plain {verbatim}; the widest measured-output line is 99
% characters, which overruns the text block at \small.
\makeatletter
\renewcommand{\verbatim@font}{\ttfamily\footnotesize}
\makeatother

% Long identifiers (hybrid_gdn_periodic, muon_polar_adamw) sit in narrow table
% columns and cannot break. Allow a break opportunity after each underscore.
% Typographic only: it inserts no character.
\makeatletter
\let\pg@origunderscore\_
\renewcommand{\_}{\pg@origunderscore\allowbreak}
\makeatother

% Blockquotes carry the corrections and retractions; make them visible.
\usepackage{framed}
\definecolor{quoteborder}{gray}{0.75}
\renewenvironment{quote}%
  {\def\FrameCommand{\textcolor{quoteborder}{\vrule width 2pt}\hspace{10pt}}%
   \MakeFramed{\advance\hsize-\width \FrameRestore}\small}%
  {\endMakeFramed}

\title{Recipe-Dependent Rankings: Method Orderings in Language-Model Screens\\
       Are Properties of the Measurement, Not the Methods}

\author{Bharath Chandra Vaddaram\\
  \small Independent researcher\\
  \small \texttt{bharath.vbcr@gmail.com}}

\date{2026-08-24}

\begin{document}
\maketitle

\input{body}

% refs.tex is generated by mkbib.py and preserves the manuscript's reference
% numbering. BibTeX would renumber -- plain sorts alphabetically, unsrt uses
% citation order, and the manuscript's order is neither -- so [12] would name a
% different paper in the PDF than in the Markdown. refs.bib is still generated
% for reference-manager import.
\input{refs}

\end{document}
